{
  "name": "optimized-translator-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.content.config.ts && vite build",
    "test": "vitest run",
    "lint": "cd .. && eslint extension/src/**/*.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/dom-chromium-ai": "^0.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.6"
  }
}
